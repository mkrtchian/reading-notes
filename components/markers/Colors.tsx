export function Orange({ children }: { children: React.ReactNode }) {
  return <strong style={{ color: "#ff9900" }}>{children}</strong>;
}

export function Pink({ children }: { children: React.ReactNode }) {
  return <strong style={{ color: "#e06666" }}>{children}</strong>;
}

export function Blue({ children }: { children: React.ReactNode }) {
  return <strong style={{ color: "#4a86e8" }}>{children}</strong>;
}

export function Yellow({ children }: { children: React.ReactNode }) {
  return <strong style={{ color: "#bf9000" }}>{children}</strong>;
}

export function Purple({ children }: { children: React.ReactNode }) {
  return <strong style={{ color: "#674ea7" }}>{children}</strong>;
}

export function Green({ children }: { children: React.ReactNode }) {
  return <strong style={{ color: "#6aa84f" }}>{children}</strong>;
}
